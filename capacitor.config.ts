import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Actividad1_9Hlc',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
